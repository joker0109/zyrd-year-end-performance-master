server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: performance-api
  
  datasource:
    url: jdbc:mysql://10.3.235.115:3306/performance_evaluation?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "eD;pL&s%g3HR"
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: PerformanceHikariCP

  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml

# JWT配置
jwt:
  secret: performance-evaluation-secret-key-2026
  expiration: 86400000  # 24小时

# 评分规则配置
performance:
  grade:
    a-min-score: 95
    b-min-score: 70
    c-min-score: 50
  vote:
    a-limit-manager: 1
    a-limit-middle: 3
    a-limit-staff: 5
  weight:
    leader: 0.4
    staff: 0.6

# 日志配置
logging:
  level:
    com.company.performance: DEBUG
    org.springframework.security: DEBUG
